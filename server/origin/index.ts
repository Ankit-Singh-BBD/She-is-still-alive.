/**
 * Her origin story: the content, and the one thing that writes it.
 *
 * `story.ts` is data — the facts themselves, parameterised by the owner's name and nothing
 * else. `seed.ts` is the only writer, idempotent by content so it can run on every boot.
 *
 * Both halves exist because of one sentence he said while she was being built: that she
 * should be able to say how she is being made, and by whom.
 */

export * from './story.js';
export * from './seed.js';
